import React from 'react';
import PublicCard from "../../components/public/PublicCard";

const Home = () => {
    return (
        <div className={'Home'}>
           <PublicCard/>
        </div>
    );
};

export default Home;